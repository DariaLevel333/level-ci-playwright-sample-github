name: Level CI Analysis
on:
  pull_request:
    branches:
      - "**"
  push:
    branches:
      - main
      - branch-cat
      - develop
      - "release/**"
      - "hotfix/**"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Install npm packages
        run: npm ci
        env:
          CLOUDSMITH_TOKEN: ${{ secrets.CLOUDSMITH_TOKEN }}

      - name: Install Playwright browsers
        run: npx playwright install chromium
      - name: Run Playwright tests
        run: npm run test:playwright

      - name: Level CI Accessibility Analysis
        uses: levelaccess/level-ci@734699300b7aea3ad3f924accb9c4ce93c0e7c77
        env:
          LEVEL_CI_TOKEN: ${{ secrets.LEVEL_CI_TOKEN }}
          NGROK_AUTHTOKEN: ${{ secrets.NGROK_AUTHTOKEN }}
        with:
          token: ${{ secrets.LEVEL_CI_TOKEN }}
          report_paths: './level-ci-reports'